package test.de.itemis.mps.editor.diagram.solution;

/*Generated by MPS */

import jetbrains.mps.MPSLaunch;
import jetbrains.mps.lang.test.runtime.BaseTransformationTest;
import org.junit.ClassRule;
import jetbrains.mps.lang.test.runtime.TestParametersCache;
import org.junit.Test;
import jetbrains.mps.lang.test.runtime.BaseEditorTestBody;
import jetbrains.mps.lang.test.runtime.TransformationTest;
import de.itemis.mps.editor.diagram.runtime.jgraph.RootDiagramECell;
import de.itemis.mps.editor.diagram.runtime.EditorUtil;
import de.itemis.mps.editor.diagram.runtime.jgraph.MyGraphComponent;
import jetbrains.mps.ide.ThreadUtils;
import org.junit.Assert;

@MPSLaunch
public class MaximizeDiagram_Test extends BaseTransformationTest {
  @ClassRule
  public static final TestParametersCache ourParamCache = new TestParametersCache(MaximizeDiagram_Test.class, "${extensions.home}/code", "r:345238d7-a9ed-4174-b957-6b04b65f40e4(test.de.itemis.mps.editor.diagram.solution@tests)", false);

  public MaximizeDiagram_Test() {
    super(ourParamCache);
  }

  @Test
  public void test_MaximizeDiagram() throws Throwable {
    new TestBody(this).testMethod();
  }

  /*package*/ static class TestBody extends BaseEditorTestBody {

    /*package*/ TestBody(TransformationTest owner) {
      super(owner);
    }

    @Override
    public void testMethodImpl() throws Exception {
      initEditorComponent("1556636692813958154", "");
      RootDiagramECell diagramECell = EditorUtil.descendants(getEditorComponent().getRootCell(), RootDiagramECell.class).get(0);
      int originalWidth = diagramECell.getWidth();
      int originalHeight = diagramECell.getHeight();

      final MyGraphComponent graphComponent = diagramECell.getGraph().getGraphComponent();
      ThreadUtils.runInUIThreadAndWait(() -> {
        graphComponent.setMaximizedMode(true);
        getEditorComponent().relayout();
      });

      int newWidth = diagramECell.getWidth();
      int newHeight = diagramECell.getHeight();

      // the new values are negative because we are in headless mode
      Assert.assertTrue(newWidth != originalWidth);
      Assert.assertTrue(newHeight != originalHeight);

      ThreadUtils.runInUIThreadAndWait(() -> {
        graphComponent.setMaximizedMode(false);
        getEditorComponent().relayout();
      });

      newWidth = diagramECell.getWidth();
      newHeight = diagramECell.getHeight();

      Assert.assertTrue(newWidth == originalWidth);
      Assert.assertTrue(newHeight == originalHeight);


    }
  }
}
