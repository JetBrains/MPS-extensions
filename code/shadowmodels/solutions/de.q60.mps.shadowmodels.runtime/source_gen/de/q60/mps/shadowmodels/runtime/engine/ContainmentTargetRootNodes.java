package de.q60.mps.shadowmodels.runtime.engine;

/*Generated by MPS */


public class ContainmentTargetRootNodes<E extends IContainmentTarget> implements IOutputNodesReference {
  private IStageReference stageRef;
  private IUniqueContainmentTarget<E> containmentTarget;

  private ContainmentTargetRootNodes() {
    // for deserialization
  }

  public ContainmentTargetRootNodes(IStageReference stageRef, IUniqueContainmentTarget<E> containmentTarget) {
    // TODO check usages of this class and replace with IOutputNodesReference if possible
    this.stageRef = stageRef;
    this.containmentTarget = containmentTarget;
  }

  public IStageReference getStageRef() {
    return this.stageRef;
  }

  public IUniqueContainmentTarget<E> getContainmentTarget() {
    return this.containmentTarget;
  }

  @Override
  public Iterable<IOutputNodeReference> getNodes(ITransformationEngine engine) {
    return engine.resolveRootNodes(this);
  }

  @Override
  public String toString() {
    return containmentTarget.toString();
  }

  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || this.getClass() != o.getClass()) {
      return false;
    }

    ContainmentTargetRootNodes that = (ContainmentTargetRootNodes) o;
    if ((stageRef != null ? !(stageRef.equals(that.stageRef)) : that.stageRef != null)) {
      return false;
    }
    if ((containmentTarget != null ? !(containmentTarget.equals(that.containmentTarget)) : that.containmentTarget != null)) {
      return false;
    }

    return true;
  }

  @Override
  public int hashCode() {
    int result = 0;
    result = 31 * result + ((stageRef != null ? ((Object) stageRef).hashCode() : 0));
    result = 31 * result + ((containmentTarget != null ? ((Object) containmentTarget).hashCode() : 0));
    return result;
  }
}
