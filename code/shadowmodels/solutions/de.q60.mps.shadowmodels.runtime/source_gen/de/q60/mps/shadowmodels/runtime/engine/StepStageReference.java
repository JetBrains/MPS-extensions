package de.q60.mps.shadowmodels.runtime.engine;

/*Generated by MPS */

import java.util.Objects;

public class StepStageReference extends StageReference {
  private int step;

  private StepStageReference() {
    // for deserialization
  }

  public StepStageReference(ISubgraphRef subgraph, int step) {
    super(subgraph);
    this.step = step;
  }

  public int getStepNumber() {
    return step;
  }

  @Override
  public String toString() {
    return subgraph + "@" + step + "_*";
  }

  private transient int _cachedHashCode;
  private transient boolean _hashCodeInitialized = false;
  @Override
  public int hashCode() {
    if (!(_hashCodeInitialized)) {
      int c = 0;
      c = 31 * c + Objects.hashCode(step);
      c = 31 * c + Objects.hashCode(subgraph);
      _cachedHashCode = c;
      _hashCodeInitialized = true;
    }
    return _cachedHashCode;
  }
  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || this.getClass() != o.getClass()) {
      return false;
    }
    StepStageReference that = (StepStageReference) o;
    if (!(Objects.equals(step, that.step))) {
      return false;
    }
    if (!(Objects.equals(subgraph, that.subgraph))) {
      return false;
    }
    return true;
  }
}
