package de.q60.mps.shadowmodels.runtime.engine;

/*Generated by MPS */

import org.modelix.model.api.INode;
import org.modelix.model.api.IConcept;
import jetbrains.mps.internal.collections.runtime.Sequence;
import org.jetbrains.annotations.Nullable;

public class NodeAsOutputNode implements IOutputNode {

  private INode node;

  public NodeAsOutputNode(INode node) {
    this.node = node;
  }

  @Override
  public IOutputNodeReference getOutputNodeReference() {
    return NodeAsOutputNodeReference.create(node);
  }
  @Override
  public IConcept getConcept() {
    return node.getConcept();
  }
  @Override
  public IContainment getContainment() {
    return new KnownContainment(NodeAsOutputNodeReference.create(node.getParent()), node.getRoleInParent());
  }
  @Override
  public Iterable<IOutputNodeReference> getChildren(String role) {
    Iterable<INode> children = node.getChildren(role);
    return Sequence.fromIterable(children).select((it) -> NodeAsOutputNodeReference.create(it));
  }
  @Override
  public Iterable<IOutputNodeReference> getAllChildren() {
    Iterable<INode> allChildren = node.getAllChildren();
    return Sequence.fromIterable(allChildren).select((it) -> NodeAsOutputNodeReference.create(it));
  }
  @Override
  public void addNewChild(String role, int index, @Nullable IConcept concept) {
    node.addNewChild(role, index, concept);
  }
  @Override
  public IOutputNodeReference getReferenceTarget(String role) {
    return NodeAsOutputNodeReference.create(node.getReferenceTarget(role));
  }
  @Override
  public void setReferenceTarget(String role, INode target) {
    node.setReferenceTarget(role, target);
  }
  @Override
  public String getPropertyValue(String role) {
    return node.getPropertyValue(role);
  }
  @Override
  public void setPropertyValue(String role, String value) {
    node.setPropertyValue(role, value);
  }

  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || this.getClass() != o.getClass()) {
      return false;
    }

    NodeAsOutputNode that = (NodeAsOutputNode) o;
    if ((node != null ? !(node.equals(that.node)) : that.node != null)) {
      return false;
    }

    return true;
  }

  @Override
  public int hashCode() {
    int result = 0;
    result = 31 * result + ((node != null ? ((Object) node).hashCode() : 0));
    return result;
  }
}
