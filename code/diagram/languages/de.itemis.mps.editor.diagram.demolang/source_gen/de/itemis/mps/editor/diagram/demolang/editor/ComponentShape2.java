package de.itemis.mps.editor.diagram.demolang.editor;

/*Generated by MPS */

import de.itemis.mps.editor.diagram.runtime.shape.AbstractShape;
import java.awt.Shape;
import java.awt.geom.Rectangle2D;
import java.awt.geom.GeneralPath;

public class ComponentShape2 extends AbstractShape {

  private double cutLength;

  public ComponentShape2(double cutLength) {
    this.cutLength = cutLength;
  }


  @Override
  public Shape getShape(final Rectangle2D bounds) {
    GeneralPath shape = new GeneralPath();
    double width = bounds.getWidth();
    double height = bounds.getHeight();
    double x = bounds.getX();
    double y = bounds.getY();
    shape.moveTo(x + 0.0, y + 0.0);
    shape.lineTo(x + 1.0 * width - this.cutLength, y + 0.0 * height);
    shape.lineTo(x + 1.0 * width, y + 0.0 * height + this.cutLength);
    shape.lineTo(x + 1.0 * width, y + 1.0 * height);
    shape.lineTo(x + 0.0 * width, y + 1.0 * height);
    shape.closePath();
    return shape;
  }



}
