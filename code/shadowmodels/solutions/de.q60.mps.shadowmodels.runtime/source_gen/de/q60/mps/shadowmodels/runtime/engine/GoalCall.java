package de.q60.mps.shadowmodels.runtime.engine;

/*Generated by MPS */

import org.modelix.model.api.INode;
import java.util.Objects;
import jetbrains.mps.internal.collections.runtime.Sequence;

public class GoalCall implements IContainmentTarget, ISubgraphCall {
  private String goalId;
  private INode input;
  private String traceInfo;

  private GoalCall() {
    // for deserialization
  }

  public GoalCall(String goalId, INode input, String traceInfo) {
    DependencyTrackingNode.checkHasTracking(input);
    this.goalId = goalId;
    this.input = input;
    this.traceInfo = traceInfo;
  }

  @Override
  public boolean hasContextParameter(String id) {
    return Objects.equals(id, goalId + ".0") || Objects.equals(id, goalId + ".input");
  }

  @Override
  public Object getContextParameter(String id) {
    return (Objects.equals(id, goalId + ".0") || Objects.equals(id, goalId + ".input") ? input : null);
  }

  public GoalCall setTraceInfo(String traceInfo) {
    this.traceInfo = traceInfo;
    return this;
  }

  @Override
  public Iterable<INode> getInputNodes() {
    return Sequence.<INode>singleton(input);
  }

  public String getGoalId() {
    return this.goalId;
  }

  public INode getInput() {
    return this.input;
  }

  public String getTraceInfo() {
    return this.traceInfo;
  }

  @Override
  public IContainmentTarget getRootCall() {
    return new RewriteCall(input, traceInfo);
  }

  @Override
  public boolean isFixpoint() {
    return true;
  }

  @Override
  public String toString() {
    return goalId + "(" + input + ")";
  }

  private transient int _cachedHashCode;
  private transient boolean _hashCodeInitialized = false;
  @Override
  public int hashCode() {
    if (!(_hashCodeInitialized)) {
      int c = 0;
      c = 31 * c + Objects.hashCode(goalId);
      c = 31 * c + Objects.hashCode(input);
      _cachedHashCode = c;
      _hashCodeInitialized = true;
    }
    return _cachedHashCode;
  }
  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || this.getClass() != o.getClass()) {
      return false;
    }
    GoalCall that = (GoalCall) o;
    if (!(Objects.equals(goalId, that.goalId))) {
      return false;
    }
    if (!(Objects.equals(input, that.input))) {
      return false;
    }
    return true;
  }
}
