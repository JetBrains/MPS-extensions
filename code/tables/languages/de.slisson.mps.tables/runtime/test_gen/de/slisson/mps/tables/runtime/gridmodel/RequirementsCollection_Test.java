package de.slisson.mps.tables.runtime.gridmodel;

/*Generated by MPS */

import org.junit.jupiter.api.Test;
import org.junit.Assert;
import org.junit.jupiter.api.BeforeEach;

public class RequirementsCollection_Test {
  private Grid main;
  @Test
  public void test_sizeX() throws Exception {
    Assert.assertEquals(3, main.getSizeX());
  }
  @Test
  public void test_sizeY() throws Exception {
    Assert.assertEquals(4, main.getSizeY());
  }
  @BeforeEach
  public void setUp() {
    main = new Grid();
    main.setElement(0, 0, buildRow(0));
    main.setElement(0, 1, buildRow(1));
    main.setElement(0, 2, buildRow(2));
    main.setElement(0, 3, buildRow(3));
    main.setColumnHeader(0, 0, new StringHeaderReference("ID"));
    main.setColumnHeader(1, 0, new StringHeaderReference("Description"));
    main.setColumnHeader(2, 0, new StringHeaderReference("Relationships"));
    main.flatten();
  }

  public Grid buildRow(int rowNum) {
    Grid row = new Grid();
    row.setElement(0, 0, buildCell("R" + rowNum, "ID"));
    row.setElement(1, 0, buildCell("Abcdef ghij " + rowNum, "Description"));
    row.setElement(2, 0, buildCell("refines " + (rowNum + 1), "Relationships"));
    row.setSpanToSize();
    return row;
  }

  public Grid buildCell(String content, String header) {
    Grid cell = new Grid();
    cell.setElement(0, 0, new GridLeaf(content));
    cell.setColumnHeader(0, 0, new StringHeaderReference(header));
    return cell;
  }
}
