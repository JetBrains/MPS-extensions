package de.itemis.mps.editor.diagram.runtime.jgraph;

/*Generated by MPS */

import de.itemis.mps.editor.diagram.runtime.model.Point;
import com.mxgraph.view.mxCellState;

public abstract class SmartGuide_BoxVertical extends SmartGuide_PointPair {
  @Override
  public Point getPointOnMoving(double dx, double dy, mxCellState moving, mxCellState other) {
    double y = getMovingY(dx, dy, moving, other);
    if (moving.getX() + moving.getWidth() + dx > other.getX()) {
      return new Point(moving.getX(), y);
    } else {
      return new Point(moving.getX() + moving.getWidth(), y);
    }
  }
  @Override
  public Point getPointOnOther(double dx, double dy, mxCellState moving, mxCellState other) {
    double y = getOtherY(dx, dy, moving, other);
    if (moving.getX() + moving.getWidth() + dx < other.getX()) {
      return new Point(other.getX(), y);
    } else {
      return new Point(other.getX() + other.getWidth(), y);
    }
  }
  @Override
  public boolean snapHorizontal() {
    return false;
  }
  @Override
  public boolean snapVertical() {
    return true;
  }

  public abstract double getMovingY(double dx, double dy, mxCellState moving, mxCellState other);
  public abstract double getOtherY(double dx, double dy, mxCellState moving, mxCellState other);
}
