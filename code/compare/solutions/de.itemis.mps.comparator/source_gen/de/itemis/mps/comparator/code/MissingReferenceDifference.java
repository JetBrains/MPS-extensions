package de.itemis.mps.comparator.code;

/*Generated by MPS */

import org.jetbrains.mps.openapi.model.SNode;
import java.util.List;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import java.util.ArrayList;
import org.jetbrains.mps.openapi.model.SReference;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SNodeOperations;
import java.util.Objects;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SLinkOperations;
import org.jetbrains.mps.openapi.language.SReferenceLink;

public class MissingReferenceDifference extends NodeDifference {
  private final String link;

  protected MissingReferenceDifference(SNode expected, SNode actual, String expectedName, String actualName, String link) {
    super(expected, actual, expectedName, actualName);
    this.link = link;
  }

  public String getLink() {
    return link;
  }

  public List<NodeDifference> getNestedDifferences() {
    return ListSequence.fromList(new ArrayList<NodeDifference>());
  }

  public boolean isMissingInExpected() {
    SReference expectedRef = extractLinkTarget(getExpected());
    return expectedRef == null;
  }

  protected SReference extractLinkTarget(SNode node) {
    return ListSequence.fromList(SNodeOperations.getReferences(node)).findFirst((it) -> Objects.equals(check_ub6wo1_a0a0a0a0a01(SLinkOperations.getRefLink(it)), getLink()));
  }



  protected String getDescription(String nameForA, String nameForB) {
    SReference actualRef = extractLinkTarget(getActual());

    SNode emptySide;
    String expectionText;
    if (isMissingInExpected()) {
      emptySide = getExpected();
      expectionText = "not expected";
    } else {
      emptySide = getActual();
      expectionText = "expected";
    }

    return "Role " + getLink() + " is " + expectionText + " in " + emptySide + "(" + emptySide.getNodeId() + ")";
  }
  private static String check_ub6wo1_a0a0a0a0a01(SReferenceLink checkedDotOperand) {
    if (null != checkedDotOperand) {
      return checkedDotOperand.getName();
    }
    return null;
  }
}
