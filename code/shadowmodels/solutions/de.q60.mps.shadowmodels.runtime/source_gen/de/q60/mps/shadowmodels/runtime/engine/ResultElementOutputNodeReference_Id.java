package de.q60.mps.shadowmodels.runtime.engine;

/*Generated by MPS */

import java.util.Objects;

public class ResultElementOutputNodeReference_Id extends ResultElementOutputNodeReference {
  private String id;

  private ResultElementOutputNodeReference_Id() {
    // for deserialization
  }

  public ResultElementOutputNodeReference_Id(ContainmentTargetRootNodes<TransformationCall> call, String id) {
    super(call);
    this.id = id;
  }

  public String getId() {
    return this.id;
  }

  @Override
  public IOutputNode resolve(ITransformationEngine engine) {
    IResultElement element = getTransformationResult(engine).getElementById(id);
    return (element == null ? null : new ResultElementAsOutputNode(engine, element));
  }

  public IResultElement resolveResultElement(ITransformationEngine engine) {
    return getTransformationResult(engine).getElementById(id);
  }

  @Override
  public IScope getScope(ITransformationEngine engine) {
    return resolveResultElement(engine).getScope();
  }

  @Override
  public IUniqueContainmentTarget getOriginalContainmentTarget() {
    return null;
  }

  @Override
  public String toString() {
    return resultRef + "#" + id;
  }

  private transient int _cachedHashCode;
  private transient boolean _hashCodeInitialized = false;
  @Override
  public int hashCode() {
    if (!(_hashCodeInitialized)) {
      int c = 0;
      c = 31 * c + Objects.hashCode(id);
      c = 31 * c + Objects.hashCode(resultRef);
      _cachedHashCode = c;
      _hashCodeInitialized = true;
    }
    return _cachedHashCode;
  }
  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || this.getClass() != o.getClass()) {
      return false;
    }
    ResultElementOutputNodeReference_Id that = (ResultElementOutputNodeReference_Id) o;
    if (!(Objects.equals(id, that.id))) {
      return false;
    }
    if (!(Objects.equals(resultRef, that.resultRef))) {
      return false;
    }
    return true;
  }
}
