package de.itemis.mps.editor.diagram.runtime.shape;

/*Generated by MPS */

import java.awt.Graphics2D;
import java.awt.geom.Rectangle2D;
import org.jetbrains.annotations.Nullable;
import java.awt.Shape;

public class CompositeShape implements IShape {

  private IShape[] myShapes;

  public CompositeShape(IShape... shapes) {
    myShapes = shapes;
  }

  public void draw(Graphics2D graphics, Rectangle2D bounds, IShapeStyle_Internal style) {
    for (IShape shape : myShapes) {
      shape.draw(graphics, bounds, style);
    }
  }

  @Nullable
  public Double getDefaultSize() {
    Double result = null;

    for (IShape shape : myShapes) {
      Double size = shape.getDefaultSize();
      if (size != null) {
        if (result == null) {
          result = size;
        } else {
          result = Math.max(result, size);
        }
      }
    }

    return result;
  }

  @Nullable
  public Shape getShape(Rectangle2D bounds) {
    return null;
  }
}
