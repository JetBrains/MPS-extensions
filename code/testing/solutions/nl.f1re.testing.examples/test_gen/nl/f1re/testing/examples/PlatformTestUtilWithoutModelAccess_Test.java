package nl.f1re.testing.examples;

/*Generated by MPS */

import jetbrains.mps.MPSLaunch;
import jetbrains.mps.lang.test.runtime.BaseTransformationTest;
import org.junit.jupiter.api.extension.RegisterExtension;
import jetbrains.mps.lang.test.runtime.TestParametersCacheExtension;
import jetbrains.mps.lang.test.runtime.TestParametersCacheBuilder;
import org.junit.jupiter.api.Test;
import jetbrains.mps.lang.test.runtime.BaseTestBody;
import jetbrains.mps.lang.test.runtime.TransformationTest;
import com.intellij.testFramework.PlatformTestUtil;
import javax.swing.JTree;
import com.intellij.openapi.application.ApplicationManager;
import org.junit.Assert;
import javax.swing.tree.DefaultMutableTreeNode;

@MPSLaunch
public class PlatformTestUtilWithoutModelAccess_Test extends BaseTransformationTest {
  @RegisterExtension
  private static final TestParametersCacheExtension ourParametersCacheExtension = new TestParametersCacheExtension(new TestParametersCacheBuilder(PlatformTestUtilWithoutModelAccess_Test.class).projectPath(null).modelRef("r:7278af02-5968-483c-a44b-8f5fe18eb6a2(nl.f1re.testing.examples@tests)").reopenProject(null).build());

  public PlatformTestUtilWithoutModelAccess_Test() {
    super(ourParametersCacheExtension.getParametersCache());
  }

  @Test
  public void test_pathsEqual() throws Throwable {
    new TestBody(this).test_pathsEqual();
  }
  @Test
  public void test_expandTree() throws Throwable {
    new TestBody(this).test_expandTree();
  }
  @Test
  public void test_allUpperCase() throws Throwable {
    new TestBody(this).test_allUpperCase();
  }
  @Test
  public void test_stdErrorSuppressed() throws Throwable {
    new TestBody(this).test_stdErrorSuppressed();
  }

  /*package*/ static class TestBody extends BaseTestBody {

    /*package*/ TestBody(TransformationTest owner) {
      super(owner);
    }

    @Override
    protected void initTestNodes() {
      prepareTestNodes();
    }

    public void test_pathsEqual() throws Exception {
      initTestNodes();
      runWithinCommand(() -> PlatformTestUtil.assertPathsEqual("folder1/file1", "folder1/file1"));
    }
    public void test_expandTree() throws Exception {
      initTestNodes();
      runWithinCommand(() -> {
        final JTree tree = new JTree(TestBody.this.createTestTreeContent());
        // To test the logical view
        ApplicationManager.getApplication().invokeAndWait(() -> {
          PlatformTestUtil.expandAll(tree);
          PlatformTestUtil.waitWhileBusy(tree);
        });

        Assert.assertEquals(6, TestBody.this.getExpandedNodeCount(tree));
      });
    }
    public void test_allUpperCase() throws Exception {
      initTestNodes();
      runWithinCommand(() -> Assert.assertTrue(PlatformTestUtil.isAllUppercaseName("HELLO")));
    }
    public void test_stdErrorSuppressed() throws Exception {
      initTestNodes();
      runWithinCommand(() -> PlatformTestUtil.withStdErrSuppressed(() -> {
        System.out.println("normal output");
        System.err.println("error output");
      }));
    }

    public DefaultMutableTreeNode createTestTreeContent() {
      DefaultMutableTreeNode root = new DefaultMutableTreeNode("Root");
      int numChildren = 5;
      for (int i = 0; i < numChildren; i++) {
        DefaultMutableTreeNode child = new DefaultMutableTreeNode("Child " + i);
        int numGrandChildren = 4;
        for (int j = 0; j < numGrandChildren; j++) {
          DefaultMutableTreeNode grandChild = new DefaultMutableTreeNode("Grandchild " + i + "." + j);
          child.add(grandChild);
        }
        root.add(child);
      }
      return root;
    }
    public int getExpandedNodeCount(JTree tree) {
      int count = 0;
      int rowCount = tree.getRowCount();
      for (int i = 0; i < rowCount; i++) {
        if (tree.isExpanded(i)) {
          count++;
        }
      }
      return count;
    }
  }
}
