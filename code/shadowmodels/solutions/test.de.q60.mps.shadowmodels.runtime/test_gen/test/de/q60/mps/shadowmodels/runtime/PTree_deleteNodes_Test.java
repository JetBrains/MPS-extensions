package test.de.q60.mps.shadowmodels.runtime;

/*Generated by MPS */

import de.q60.mps.shadowmodels.runtime.model.persistent.SM_PTree;
import org.junit.Test;
import org.junit.Assert;
import jetbrains.mps.internal.collections.runtime.Sequence;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import java.util.ArrayList;
import org.junit.Before;
import org.modelix.model.api.IConcept;
import com.google.common.primitives.Longs;
import jetbrains.mps.internal.collections.runtime.IWhereFilter;

public class PTree_deleteNodes_Test {
  private SM_PTree tree1;
  private SM_PTree tree2;
  @Test
  public void test_size_before() throws Exception {
    Assert.assertEquals(10, Sequence.fromIterable(tree1.getChildren(SM_PTree.ROOT_ID, "a")).count());
  }
  @Test
  public void test_values_before() throws Exception {
    Assert.assertEquals(ListSequence.fromListAndArray(new ArrayList(), 1000L, 1001L, 1002L, 1003L, 1004L, 1005L, 1006L, 1007L, 1008L, 1009L), ListSequence.fromListWithValues(new ArrayList(), tree1.getChildren(SM_PTree.ROOT_ID, "a")));
  }
  @Test
  public void test_size_after() throws Exception {
    Assert.assertEquals(7, Sequence.fromIterable(tree2.getChildren(SM_PTree.ROOT_ID, "a")).count());
  }
  @Test
  public void test_values_after() throws Exception {
    Assert.assertEquals(ListSequence.fromListAndArray(new ArrayList(), 1000L, 1001L, 1003L, 1004L, 1006L, 1007L, 1009L), ListSequence.fromListWithValues(new ArrayList(), tree2.getChildren(SM_PTree.ROOT_ID, "a")));
  }
  @Before
  public void setUp() {
    tree1 = SM_PTree.EMPTY;
    for (int i = 0; i < 10; i++) {
      tree1 = tree1.addNewChild(SM_PTree.ROOT_ID, "a", -1, 1000L + i, (IConcept) null);
    }

    tree2 = tree1;
    long[] toDelete = Longs.toArray(Sequence.fromIterable(tree2.getChildren(SM_PTree.ROOT_ID, "a")).where(new IWhereFilter<Long>() {
      public boolean accept(Long it) {
        return it % 3 == 0;
      }
    }).toListSequence());
    tree2 = tree2.deleteNodes(toDelete);
  }
}
