package de.itemis.mps.editor.math.symbols;

/*Generated by MPS */

import de.itemis.mps.editor.math.runtime.AbstractMathSymbol;
import java.awt.Graphics2D;
import java.awt.geom.Rectangle2D;
import de.itemis.mps.editor.math.runtime.MathDrawUtil;
import de.itemis.mps.editor.math.runtime.Dimension2DDouble;

public class ProductSymbol extends AbstractMathSymbol {

  public ProductSymbol() {
  }

  protected void paint(Graphics2D g, Rectangle2D bounds) {
    MathDrawUtil util = new MathDrawUtil(g);
    g.setStroke(util.getThinStroke(bounds));

    util.drawLineLeft(bounds, ((int) (bounds.getWidth() * 0.15)));
    util.drawLineRight(bounds, ((int) (bounds.getWidth() * 0.15)));
    g.setStroke(util.getThickStroke(bounds));
    util.drawLineTop(bounds);
  }
  protected void updateDimension(Dimension2DDouble dimension) {
    dimension.width = Math.pow(dimension.height, 0.8);
  }
}
