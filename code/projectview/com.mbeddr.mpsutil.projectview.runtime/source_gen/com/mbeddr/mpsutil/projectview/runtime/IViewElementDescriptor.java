package com.mbeddr.mpsutil.projectview.runtime;

/*Generated by MPS */

import jetbrains.mps.project.Project;
import org.jetbrains.annotations.Nullable;
import javax.swing.Icon;
import com.intellij.ui.SimpleTextAttributes;

public interface IViewElementDescriptor<E> {
  ElementId getId();
  ElementId getParentId();
  ElementId getExtendsId();
  boolean isAbstract();
  boolean isOverride();
  boolean isQueryable();
  Iterable<ViewElement<E>> queryElements(Project mpsProject, ViewElement parentElement, ViewId contextView);
  String getLabel(ViewElement<E> element);
  @Nullable
  Icon getIcon(ViewElement<E> element);
  @Nullable
  SimpleTextAttributes getTextAttributes(ViewElement<E> element);
  String getActionGroupId(ViewElement<E> element);
  boolean isApplicable(Project mpsProject, ViewElement parentElement);
  boolean isAssignable(Project mpsProject, ViewElement<E> element);
  Iterable<String> getFolders(ViewElement<E> element);
  String getFoldersActiondGroupId(ViewElement<E> element);
  @Nullable
  Class getOutputType();
  Boolean loadLazy(ViewElement<E> element);
  Boolean loadAsync(ViewElement<E> element);
  Boolean isAutoUpdate(ViewElement<E> element);
}
