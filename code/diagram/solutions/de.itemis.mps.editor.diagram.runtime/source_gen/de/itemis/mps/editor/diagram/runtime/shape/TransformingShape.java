package de.itemis.mps.editor.diagram.runtime.shape;

/*Generated by MPS */

import java.awt.geom.AffineTransform;
import java.awt.Graphics2D;
import java.awt.geom.Rectangle2D;
import org.jetbrains.annotations.Nullable;
import java.awt.Shape;

public class TransformingShape implements IShape {
  private IShape myShape;
  private AffineTransform myTransform;

  public TransformingShape(IShape shape) {
    this(shape, null);
  }

  public TransformingShape(IShape shape, AffineTransform transform) {
    myShape = shape;
    myTransform = transform;
  }

  public void draw(Graphics2D graphics, Rectangle2D bounds, IShapeStyle_Internal style) {
    AffineTransform transform = getTransform();
    if (transform != null) {
      graphics.translate(bounds.getCenterX(), bounds.getCenterY());
      graphics.transform(getTransform());
      graphics.translate(-bounds.getCenterX(), -bounds.getCenterY());
    }
    myShape.draw(graphics, bounds, style);
  }

  @Nullable
  protected AffineTransform getTransform() {
    return myTransform;
  }

  @Nullable
  public Double getDefaultSize() {
    return null;
  }

  @Nullable
  public Shape getShape(Rectangle2D bounds) {
    return null;
  }
}
