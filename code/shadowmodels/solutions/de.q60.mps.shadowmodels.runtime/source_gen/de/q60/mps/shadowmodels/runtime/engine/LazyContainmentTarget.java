package de.q60.mps.shadowmodels.runtime.engine;

/*Generated by MPS */

import org.modelix.model.api.INode;
import java.util.Objects;

public class LazyContainmentTarget<E extends IContainmentTarget> implements IUniqueContainmentTarget<E> {

  private E target;

  private LazyContainmentTarget() {
    // for deserialization
  }

  public LazyContainmentTarget(E target) {
    this.target = target;
  }


  @Override
  public E getTarget() {
    return target;
  }

  @Override
  public Iterable<INode> getInputNodes() {
    return target.getInputNodes();
  }

  @Override
  public String toString() {
    return target.toString();
  }

  @Override
  public IScope getScope() {
    throw new UnsupportedOperationException();
  }

  private transient int _cachedHashCode;
  private transient boolean _hashCodeInitialized = false;
  @Override
  public int hashCode() {
    if (!(_hashCodeInitialized)) {
      int c = 0;
      c = 31 * c + Objects.hashCode(target);
      _cachedHashCode = c;
      _hashCodeInitialized = true;
    }
    return _cachedHashCode;
  }
  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || this.getClass() != o.getClass()) {
      return false;
    }
    LazyContainmentTarget that = (LazyContainmentTarget) o;
    if (!(Objects.equals(target, that.target))) {
      return false;
    }
    return true;
  }
}
