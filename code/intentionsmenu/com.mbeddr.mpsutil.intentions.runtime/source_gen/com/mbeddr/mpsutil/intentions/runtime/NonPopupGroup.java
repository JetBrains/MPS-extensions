package com.mbeddr.mpsutil.intentions.runtime;

/*Generated by MPS */

import com.intellij.openapi.actionSystem.ActionGroupWrapper;
import org.jetbrains.annotations.NotNull;
import com.intellij.openapi.actionSystem.ActionGroup;
import com.intellij.openapi.actionSystem.AnActionEvent;

/**
 * A group wrapper that forces the wrapped group to become non-popup.
 */
/*package*/ class NonPopupGroup extends ActionGroupWrapper {
  public NonPopupGroup(@NotNull ActionGroup action) {
    super(action);
  }

  @Override
  public void update(@NotNull AnActionEvent e) {
    super.update(e);
    // Resetting the popup flag on template presentation doesn't seem to work so we reset it on every update instead.
    e.getPresentation().setPopupGroup(false);
  }
}
