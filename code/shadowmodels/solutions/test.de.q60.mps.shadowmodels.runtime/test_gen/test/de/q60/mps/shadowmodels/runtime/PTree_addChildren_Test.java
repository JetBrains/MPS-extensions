package test.de.q60.mps.shadowmodels.runtime;

/*Generated by MPS */

import de.q60.mps.shadowmodels.runtime.model.persistent.SM_PTree;
import org.modelix.model.api.IConcept;
import org.junit.Test;
import org.junit.Assert;
import jetbrains.mps.internal.collections.runtime.Sequence;
import jetbrains.mps.internal.collections.runtime.ArrayUtils;
import org.junit.Before;
import de.q60.mps.shadowmodels.runtime.model.persistent.SM_DefaultIdGenerator;

public class PTree_addChildren_Test {
  private SM_PTree tree;
  private long[] ids;
  private IConcept[] concepts;
  @Test
  public void test_size() throws Exception {
    Assert.assertEquals(3, Sequence.fromIterable(tree.getChildren(SM_PTree.ROOT_ID, "a")).count());
  }
  @Test
  public void test_values() throws Exception {
    Assert.assertEquals(Sequence.fromIterable(ArrayUtils.fromLongArray(ids)).toListSequence(), Sequence.fromIterable(tree.getChildren(SM_PTree.ROOT_ID, "a")).toListSequence());
  }
  @Before
  public void setUp() {
    tree = SM_PTree.EMPTY;
    ids = new long[3];
    concepts = new IConcept[ids.length];
    for (int i = 0; i < ids.length; i++) {
      ids[i] = SM_DefaultIdGenerator.getInstance().generate();
      concepts[i] = null;
    }
    tree = tree.addNewChildren(SM_PTree.ROOT_ID, "a", -1, ids, concepts);
  }
}
