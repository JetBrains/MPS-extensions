package de.itemis.mps.blutil.sandbox;

/*Generated by MPS */

import jetbrains.mps.internal.collections.runtime.ListSequence;
import java.util.ArrayList;
import java.util.Map;
import java.util.List;
import com.mbeddr.mpsutil.blutil.rt.operations.GroupByOperationUtil;
import jetbrains.mps.internal.collections.runtime.MapSequence;
import jetbrains.mps.internal.collections.runtime.Sequence;

public class GroupByOperationSandbox {
  public static void usage() {
    Iterable<String> numbers = ListSequence.fromListAndArray(new ArrayList<String>(), "One", "Two", "Three", "Four", "Five", "Six", "Seven", "Eight", "Nine");
    Map<Integer, List<String>> byLength = GroupByOperationUtil.apply(numbers, (String elem) -> elem.length());
    System.out.println(MapSequence.fromMap(byLength).count());
    Map<Boolean, List<String>> isVowel = GroupByOperationUtil.apply(numbers, (String it) -> StringUtils.substring(it, 0, 1).matches("[AEIOU]"));
    System.out.println(MapSequence.fromMap(isVowel).get(true));
    Sequence.fromIterable(numbers).where((it) -> it.length() < 5);
  }
}
