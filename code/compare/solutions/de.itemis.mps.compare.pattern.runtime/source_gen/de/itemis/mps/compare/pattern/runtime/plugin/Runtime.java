package de.itemis.mps.compare.pattern.runtime.plugin;

/*Generated by MPS */

import java.util.Collections;

public final class Runtime {

  public static <T> T safeGetSingle(MatchResult result, String name, Class<T> clazz) {
    Object value = result.get(name);
    if (value != null && clazz.isInstance(value)) {
      return (T) value;
    } else {
      return null;
    }
  }

  public static <T> Iterable<T> safeGetIterable(MatchResult result, String name, Class<T> clazz) {
    Object value = result.get(name);
    if (value != null && value instanceof Iterable) {
      return (Iterable<T>) value;
    } else {
      return Collections.<T>emptySet();
    }
  }

}
