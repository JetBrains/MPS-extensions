package de.q60.mps.shadowmodels.debugview.pf;

/*Generated by MPS */

import javax.swing.Icon;
import java.awt.Component;
import java.awt.Graphics;
import java.awt.Graphics2D;
import java.awt.RenderingHints;
import com.intellij.ui.JBColor;
import java.awt.BasicStroke;
import java.awt.geom.Ellipse2D;
import java.awt.geom.Line2D;

public class ScopeIcon implements Icon {
  public static final ScopeIcon INSTANCE = new ScopeIcon();
  @Override
  public void paintIcon(Component component, Graphics g_, int x, int y) {
    Graphics2D g = (Graphics2D) g_.create();
    try {
      g.setRenderingHint(RenderingHints.KEY_ANTIALIASING, RenderingHints.VALUE_ANTIALIAS_ON);
      g.translate(x, y);
      g.scale(getIconWidth(), getIconHeight());
      g.setColor(JBColor.BLACK);
      g.setStroke(new BasicStroke(1.0f / getIconWidth()));
      g.draw(new Ellipse2D.Double(0.1, 0.1, 0.8, 0.8));
      g.draw(new Line2D.Double(0.5, 0.0, 0.5, 0.3));
      g.draw(new Line2D.Double(0.5, 0.7, 0.5, 1.0));
      g.draw(new Line2D.Double(0.0, 0.5, 0.3, 0.5));
      g.draw(new Line2D.Double(0.7, 0.5, 1.0, 0.5));
    } finally {
      g.dispose();
    }
  }
  @Override
  public int getIconWidth() {
    return 16;
  }
  @Override
  public int getIconHeight() {
    return 16;
  }
}
