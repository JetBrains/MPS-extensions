package de.q60.mps.shadowmodels.runtime.engine;

/*Generated by MPS */

import java.util.Objects;

public class OutputNodeAsReferenceTarget implements IReferenceTarget {
  private IOutputNodeReference outputNode;

  private OutputNodeAsReferenceTarget() {
    // for deserialization
  }

  public OutputNodeAsReferenceTarget(IOutputNodeReference outputNode) {
    this.outputNode = outputNode;
  }

  @Override
  public IOutputNodeReference resolve(IScope sourceScope, ITransformationEngine engine) {
    return outputNode;
  }

  private transient int _cachedHashCode;
  private transient boolean _hashCodeInitialized = false;
  @Override
  public int hashCode() {
    if (!(_hashCodeInitialized)) {
      int c = 0;
      c = 31 * c + Objects.hashCode(outputNode);
      _cachedHashCode = c;
      _hashCodeInitialized = true;
    }
    return _cachedHashCode;
  }
  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || this.getClass() != o.getClass()) {
      return false;
    }
    OutputNodeAsReferenceTarget that = (OutputNodeAsReferenceTarget) o;
    if (!(Objects.equals(outputNode, that.outputNode))) {
      return false;
    }
    return true;
  }
}
