package com.mbeddr.mpsutil.grammarcells.runtime;

/*Generated by MPS */

import org.jetbrains.annotations.Nullable;

/**
 * 
 * One way of reaching a ChartItem - the item with one symbol less ('myPrev'), followed by
 * the match of that symbol, which is either a completed item ('myChild') or a single input
 * token ('myChild' is null then). An ambiguous input produces several links for one item.
 */
public class ChartLink {
  private ChartItem myPrev;
  private ChartItem myChild;

  public ChartLink(ChartItem prev, @Nullable ChartItem child) {
    myPrev = prev;
    myChild = child;
  }

  public ChartItem getPrev() {
    return myPrev;
  }

  /**
   * The subtree that matched the symbol, or null if it was matched by a single token.
   */
  @Nullable
  public ChartItem getChild() {
    return myChild;
  }

  @Override
  public String toString() {
    return "" + myPrev + " + " + ((myChild == null ? "token" : "" + myChild));
  }
}
