package de.itemis.mps.compare.pattern.runtime.plugin;

/*Generated by MPS */


public abstract class PatternBuilderInitPart<T> implements IPatternEvaluator {

  protected Expression<T> inner;
  protected String annotation;
  protected boolean negated;

  public PatternBuilderInitPart(Expression<T> inner, String annotation, boolean negated) {
    this.inner = inner;
    this.annotation = annotation;
    this.negated = negated;
  }

  public PatternBuilderInitPart(Expression<T> inner, String annotation) {
    this.inner = inner;
    this.annotation = annotation;
  }

  public PatternBuilderInitPart(Expression<T> inner) {
    this(inner, null);
  }

  protected boolean shouldAnnotate() {
    return this.annotation != null && isNotEmptyString(this.annotation);
  }
  private static boolean isNotEmptyString(String str) {
    return str != null && str.length() > 0;
  }
}
