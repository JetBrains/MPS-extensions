package de.q60.mps.shadowmodels.runtime.engine;

/*Generated by MPS */


public class StepNumber {
  private final int step;
  private final int substep;

  public StepNumber(int step, int substep) {
    if (step < 0 || substep < 0) {
      throw new IllegalArgumentException(step + ", " + substep);
    }
    this.step = step;
    this.substep = substep;
  }

  public boolean isLowerThan(StepNumber other) {
    if (step < other.step) {
      return true;
    }
    if (step > other.step) {
      return false;
    }
    return substep < other.substep;
  }

  public StepNumber incrementSubstep() {
    return new StepNumber(step, substep + 1);
  }

  public StepNumber decrementSubstep() {
    return new StepNumber(step, substep - 1);
  }

  public StepNumber incrementStep() {
    return new StepNumber(step + 1, substep);
  }

  @Override
  public String toString() {
    return step + "_" + substep;
  }

  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || this.getClass() != o.getClass()) {
      return false;
    }

    StepNumber that = (StepNumber) o;
    if (step != that.step) {
      return false;
    }
    if (substep != that.substep) {
      return false;
    }

    return true;
  }

  @Override
  public int hashCode() {
    int result = 0;
    result = 31 * result + step;
    result = 31 * result + substep;
    return result;
  }

  public int getStep() {
    return this.step;
  }

  public int getSubstep() {
    return this.substep;
  }
}
