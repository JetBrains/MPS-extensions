package de.q60.mps.shadowmodels.runtime.engine;

/*Generated by MPS */

import java.util.Objects;

public class LazySubgraphStageReference implements ILazyStageRef {
  protected ILazySubgraphRef subgraph;

  private LazySubgraphStageReference() {
    // for deserialization
  }

  public LazySubgraphStageReference(ILazySubgraphRef subgraph) {
    this.subgraph = subgraph;
  }

  @Override
  public ILazySubgraphRef getSubgraphRef() {
    return subgraph;
  }

  @Override
  public boolean isEager() {
    return false;
  }

  @Override
  public boolean allowLazyReferenceTargets() {
    return true;
  }

  private transient int _cachedHashCode;
  private transient boolean _hashCodeInitialized = false;
  @Override
  public int hashCode() {
    if (!(_hashCodeInitialized)) {
      int c = 0;
      c = 31 * c + Objects.hashCode(subgraph);
      _cachedHashCode = c;
      _hashCodeInitialized = true;
    }
    return _cachedHashCode;
  }
  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || this.getClass() != o.getClass()) {
      return false;
    }
    LazySubgraphStageReference that = (LazySubgraphStageReference) o;
    if (!(Objects.equals(subgraph, that.subgraph))) {
      return false;
    }
    return true;
  }

  @Override
  public String toString() {
    return subgraph.toString();
  }
}
