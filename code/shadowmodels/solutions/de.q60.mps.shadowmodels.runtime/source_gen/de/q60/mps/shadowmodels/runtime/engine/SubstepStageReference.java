package de.q60.mps.shadowmodels.runtime.engine;

/*Generated by MPS */

import java.util.Objects;

public class SubstepStageReference extends StageReference {
  private int step;
  private int substep;

  private SubstepStageReference() {
    // for deserialization
  }

  public SubstepStageReference(ISubgraphRef subgraph, int step, int substep) {
    super(subgraph);
    this.step = step;
    this.substep = substep;
  }

  public int getStepNumber() {
    return step;
  }

  public int getSubstepNumber() {
    return substep;
  }

  public SubstepStageReference nextSubstep() {
    return new SubstepStageReference(getSubgraphRef(), step, substep + 1);
  }

  public SubstepStageReference nextStep() {
    return new SubstepStageReference(getSubgraphRef(), step + 1, 0);
  }

  @Override
  public String toString() {
    return subgraph + "@" + step + "_" + substep;
  }

  private transient int _cachedHashCode;
  private transient boolean _hashCodeInitialized = false;
  @Override
  public int hashCode() {
    if (!(_hashCodeInitialized)) {
      int c = 0;
      c = 31 * c + Objects.hashCode(step);
      c = 31 * c + Objects.hashCode(substep);
      c = 31 * c + Objects.hashCode(subgraph);
      _cachedHashCode = c;
      _hashCodeInitialized = true;
    }
    return _cachedHashCode;
  }
  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || this.getClass() != o.getClass()) {
      return false;
    }
    SubstepStageReference that = (SubstepStageReference) o;
    if (!(Objects.equals(step, that.step))) {
      return false;
    }
    if (!(Objects.equals(substep, that.substep))) {
      return false;
    }
    if (!(Objects.equals(subgraph, that.subgraph))) {
      return false;
    }
    return true;
  }
}
