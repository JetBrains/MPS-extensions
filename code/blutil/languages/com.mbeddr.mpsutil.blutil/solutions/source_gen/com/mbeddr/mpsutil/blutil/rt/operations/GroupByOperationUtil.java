package com.mbeddr.mpsutil.blutil.rt.operations;

/*Generated by MPS */

import java.util.Map;
import java.util.List;
import jetbrains.mps.baseLanguage.closures.runtime._FunctionTypes;
import jetbrains.mps.internal.collections.runtime.MapSequence;
import java.util.HashMap;
import jetbrains.mps.internal.collections.runtime.Sequence;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import java.util.ArrayList;

public class GroupByOperationUtil {
  public static <T, K> Map<K, List<T>> apply(Iterable<T> seq, final _FunctionTypes._return_P1_E0<? extends K, ? super T> keyExtract) {
    final Map<K, List<T>> cache = MapSequence.fromMap(new HashMap<K, List<T>>());
    Sequence.fromIterable(seq).visitAll((it) -> {
      K key = keyExtract.invoke(it);
      if (!(MapSequence.fromMap(cache).containsKey(key))) {
        MapSequence.fromMap(cache).put(key, ListSequence.fromList(new ArrayList<T>()));
      }
      ListSequence.fromList(MapSequence.fromMap(cache).get(key)).addElement(it);
    });
    return cache;
  }
}
