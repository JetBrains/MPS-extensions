package de.itemis.model.merge.test.gen;

/*Generated by MPS */

import org.jetbrains.mps.openapi.model.SModel;
import org.jetbrains.mps.openapi.model.SNode;
import jetbrains.mps.smodel.tempmodel.TemporaryModels;
import jetbrains.mps.smodel.tempmodel.TempModuleOptions;
import org.junit.Assert;
import com.google.common.collect.Lists;
import org.jetbrains.mps.openapi.model.SNodeReference;
import org.jetbrains.mps.openapi.module.SRepository;
import jetbrains.mps.project.ProjectManager;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SPointerOperations;

public class TestUtil {

  public static SModel makeTempModelFrom(SNode tSystem) {
    SModel model = TemporaryModels.getInstance().createEditable(false, TempModuleOptions.forDefaultModule());
    Assert.assertEquals(0, Lists.newArrayList(model.getRootNodes()).size());
    model.addRootNode(tSystem);
    Assert.assertEquals(1, Lists.newArrayList(model.getRootNodes()).size());
    return model;
  }

  public static SNode modelMergeFrom(SNodeReference nodePtr) {
    SRepository repository = ProjectManager.getInstance().getOpenedProjects().iterator().next().getRepository();
    Assert.assertNotNull(repository);

    SNode modelMerge = SPointerOperations.resolveNode(nodePtr, repository);
    Assert.assertNotNull(modelMerge);
    return modelMerge;
  }

}
