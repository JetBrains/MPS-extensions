package de.itemis.mps.editor.pagination.runtime.pages;

/*Generated by MPS */


public class PageNumber implements PageIterator<PageNumber> {
  private final int number;

  public PageNumber(int number) {
    if (number <= 0) {
      throw new IllegalArgumentException("Page number must be positive and greater than 0.");
    }
    this.number = number;
  }

  public PageNumber() {
    this(1);
  }

  public int getValue() {
    return number;
  }

  public boolean hasNext() {
    return true;
  }

  public PageNumber next() {
    return new PageNumber(getValue() + 1);
  }

  public boolean hasPrevious() {
    return (getValue() > 1);
  }

  public PageNumber previous() {
    return (hasPrevious() ? new PageNumber(getValue() - 1) : this);
  }

  @Override
  public boolean equals(Object obj) {
    if (obj instanceof PageNumber) {
      return ((PageNumber) obj).getValue() == getValue();
    }
    return super.equals(obj);
  }

  @Override
  public int hashCode() {
    return getValue();
  }

  @Override
  public String toString() {
    return Integer.toString(getValue());
  }
}
