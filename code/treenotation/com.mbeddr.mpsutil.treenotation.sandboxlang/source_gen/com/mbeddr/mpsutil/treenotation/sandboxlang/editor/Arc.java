package com.mbeddr.mpsutil.treenotation.sandboxlang.editor;

/*Generated by MPS */

import com.mbeddr.mpsutil.treenotation.runtime.IShape;
import java.awt.Graphics2D;
import java.util.List;
import java.awt.geom.Line2D;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import java.awt.geom.Arc2D;

public class Arc implements IShape {
  private double size;

  public Arc(double size) {
    this.size = size;
  }

  @Override
  public void paint(Graphics2D g, List<Line2D> edges) {
    if (ListSequence.fromList(edges).count() < 2) {
      return;
    }

    Line2D first = ListSequence.fromList(edges).first();
    Line2D last = ListSequence.fromList(edges).last();
    // assumption: first.p2 == last.p2
    Arc2D.Double arc = new Arc2D.Double();
    arc.setArcByCenter(first.getP2().getX(), first.getP2().getY(), size, 0, 0, Arc2D.PIE);
    arc.setAngles(first.getP1(), last.getP1());
    g.fill(arc);

  }
}
