package de.itemis.mps.editor.celllayout.boxmodel;

/*Generated by MPS */


public class DefaultBoxModel implements BoxModel {
  protected LayoutBox myContentBox;
  protected LayoutBoxFrame myPaddingBox;
  protected LayoutBoxFrame myBorderBox;
  protected LayoutBoxFrame myMarginBox;

  public DefaultBoxModel() {
    init();
  }

  protected void init() {
    myContentBox = new DefaultLayoutBox();
    myPaddingBox = new DefaultLayoutBoxFrame(myContentBox);
    myBorderBox = new DefaultLayoutBoxFrame(myPaddingBox);
    myMarginBox = new DefaultLayoutBoxFrame(myBorderBox);
  }

  @Override
  public LayoutBox getContentBox() {
    return myContentBox;
  }

  @Override
  public LayoutBoxFrame getPaddingBox() {
    return myPaddingBox;
  }

  @Override
  public LayoutBoxFrame getBorderBox() {
    return myBorderBox;
  }

  @Override
  public LayoutBoxFrame getMarginBox() {
    return myMarginBox;
  }

  @Override
  public DefaultBoxModel clone() {
    try {
      DefaultBoxModel clone = ((DefaultBoxModel) super.clone());
      clone.myMarginBox = myMarginBox.clone();
      clone.myBorderBox = ((LayoutBoxFrame) clone.myMarginBox.getInnerBox());
      clone.myPaddingBox = ((LayoutBoxFrame) clone.myBorderBox.getInnerBox());
      clone.myContentBox = clone.myPaddingBox.getInnerBox();
      return clone;
    } catch (CloneNotSupportedException e) {
      throw new RuntimeException(e);
    }
  }

  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || this.getClass() != o.getClass()) {
      return false;
    }

    DefaultBoxModel that = (DefaultBoxModel) o;
    if ((myMarginBox != null ? !(myMarginBox.equals(that.myMarginBox)) : that.myMarginBox != null)) {
      return false;
    }

    return true;
  }

  @Override
  public int hashCode() {
    int result = 0;
    result = 31 * result + ((myMarginBox != null ? ((Object) myMarginBox).hashCode() : 0));
    return result;
  }
}
