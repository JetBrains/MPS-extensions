package nl.f1re.testing.examples;

/*Generated by MPS */

import jetbrains.mps.MPSLaunch;
import jetbrains.mps.lang.test.runtime.BaseTransformationTest;
import org.junit.jupiter.api.extension.RegisterExtension;
import jetbrains.mps.lang.test.runtime.TestParametersCacheExtension;
import jetbrains.mps.lang.test.runtime.TestParametersCacheBuilder;
import org.junit.jupiter.api.Test;
import jetbrains.mps.lang.test.runtime.BaseTestBody;
import jetbrains.mps.lang.test.runtime.TransformationTest;
import jetbrains.mps.nodeEditor.EditorComponent;
import jetbrains.mps.nodeEditor.NodeEditorComponent;
import java.util.concurrent.atomic.AtomicBoolean;
import com.intellij.diagnostic.PerformanceWatcherImpl;
import com.intellij.diagnostic.PerformanceWatcher;
import com.intellij.diagnostic.PerformanceListener;
import org.jetbrains.annotations.NotNull;
import java.nio.file.Path;
import kotlinx.coroutines.CoroutineScope;
import org.jetbrains.annotations.Nullable;
import com.intellij.openapi.extensions.ExtensionPointName;
import java.lang.reflect.Field;
import com.intellij.diagnostic.PerformanceWatcherImplKt;
import com.intellij.openapi.util.Disposer;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SPropertyOperations;
import com.intellij.util.WaitFor;
import org.junit.Assert;
import org.jetbrains.mps.openapi.language.SProperty;
import jetbrains.mps.smodel.adapter.structure.MetaAdapterFactory;

@MPSLaunch
public class SlowEditor_Test extends BaseTransformationTest {
  @RegisterExtension
  private static final TestParametersCacheExtension ourParametersCacheExtension = new TestParametersCacheExtension(new TestParametersCacheBuilder(SlowEditor_Test.class).projectPath("${extensions.home}/code").modelRef("r:7278af02-5968-483c-a44b-8f5fe18eb6a2(nl.f1re.testing.examples@tests)").reopenProject(false).build());

  public SlowEditor_Test() {
    super(ourParametersCacheExtension.getParametersCache());
  }

  @Test
  public void test_detectSlowEditor() throws Throwable {
    new TestBody(this).test_detectSlowEditor();
  }

  /*package*/ static class TestBody extends BaseTestBody {

    /*package*/ TestBody(TransformationTest owner) {
      super(owner);
    }

    @Override
    protected void initTestNodes() {
      prepareTestNodes("6801793966041436988");
    }

    public void test_detectSlowEditor() throws Exception {
      initTestNodes();
      runWithinCommand(() -> {
        // FIXME Always true. A different freeze is found
        EditorComponent component = new NodeEditorComponent(myProject.getRepository());
        final AtomicBoolean eventTriggered = new AtomicBoolean(false);

        PerformanceWatcherImpl watcher = as_kaiu9d_a0a4a0a1a5g(PerformanceWatcher.getInstance(), PerformanceWatcherImpl.class);
        PerformanceListener performanceListener = new PerformanceListener() {
          @Override
          public void uiFreezeStarted(@NotNull Path reportDir, @NotNull CoroutineScope coroutineScope) {
            System.out.println("freeze started");
            eventTriggered.set(true);
          }

          @Override
          public void uiFreezeFinished(long durationMs, @Nullable Path reportDir) {
            System.out.println("freeze finished");
          }
          @Override
          public void uiFreezeRecorded(long durationMs, @Nullable Path reportDir) {
            System.out.println("freeze recorded");
          }
        };
        ExtensionPointName<PerformanceListener> EP_NAME = null;

        try {
          Field epField = PerformanceWatcherImplKt.class.getDeclaredField("EP_NAME");
          epField.setAccessible(true);
          EP_NAME = ((ExtensionPointName<PerformanceListener>) epField.get(watcher));
          EP_NAME.getPoint().registerExtension(performanceListener, Disposer.newDisposable());
          // It is set to false but there is still a freeze
          SPropertyOperations.assign(getAnnotatedNode("slowMode"), PROPS.slowMode$sY5T, false);
          component.editNode(getAnnotatedNode("slowMode"));
          boolean result = new WaitFor(15000) {
            @Override
            protected boolean condition() {
              return eventTriggered.get();
            }
          }.condition();
          Assert.assertTrue(result);

        } catch (NoSuchFieldException e) {
        } catch (IllegalAccessException e) {
        } finally {
          if (EP_NAME != null) {
            EP_NAME.getPoint().unregisterExtension(PerformanceListener.class);
          }
        }
      });
    }

    private static <T> T as_kaiu9d_a0a4a0a1a5g(Object o, Class<T> type) {
      return (type.isInstance(o) ? (T) o : null);
    }
  }

  private static final class PROPS {
    /*package*/ static final SProperty slowMode$sY5T = MetaAdapterFactory.getProperty(0x87e083b3d1b34c3fL, 0x9d8cb24d74710f49L, 0x5e64d3931d4923d4L, 0x5e64d3931d4b05bdL, "slowMode");
  }
}
