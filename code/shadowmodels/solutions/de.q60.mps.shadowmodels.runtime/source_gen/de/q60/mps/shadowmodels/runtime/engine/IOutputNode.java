package de.q60.mps.shadowmodels.runtime.engine;

/*Generated by MPS */

import org.modelix.model.api.IConcept;
import org.jetbrains.annotations.Nullable;
import org.modelix.model.api.INode;

/**
 * IOutputNode shouldn't do any further resolving.
 * It can hold references to data returned by the incremental engine.
 * There shouldn't be a wrapper from IOutputNode to INode.
 * Calls on INode should always go through the incremental/transformation engine.
 */
public interface IOutputNode {
  IOutputNodeReference getOutputNodeReference();

  IConcept getConcept();
  IContainment getContainment();

  Iterable<IOutputNodeReference> getChildren(String role);
  Iterable<IOutputNodeReference> getAllChildren();
  void addNewChild(String role, int index, @Nullable IConcept concept);

  IOutputNodeReference getReferenceTarget(String role);
  void setReferenceTarget(String role, INode target);

  String getPropertyValue(String role);
  void setPropertyValue(String role, String value);

}
