package de.slisson.mps.richtext.plugin;

/*Generated by MPS */

import jetbrains.mps.workbench.action.BaseAction;
import javax.swing.Icon;
import jetbrains.mps.ide.editor.EditorActionAccess;
import com.intellij.openapi.actionSystem.AnActionEvent;
import java.util.Map;
import de.slisson.mps.richtext.runtime.selection.RichtextHelper;
import jetbrains.mps.ide.editor.MPSEditorDataKeys;
import de.slisson.mps.editor.multiline.cells.EditorCell_Word;
import org.jetbrains.annotations.NotNull;
import jetbrains.mps.nodeEditor.EditorComponent;

public class DeleteToWordStart_Action extends BaseAction {
  private static final Icon ICON = null;

  public DeleteToWordStart_Action() {
    super("Delete to Word Start", "", ICON);
    this.setIsAlwaysVisible(false);
    this.setActionAccess(EditorActionAccess.UNDO_EDITOR);
  }
  @Override
  public boolean isDumbAware() {
    return true;
  }
  @Override
  public boolean isApplicable(AnActionEvent event, final Map<String, Object> _params) {
    RichtextHelper helper = new RichtextHelper(event.getData(MPSEditorDataKeys.EDITOR_COMPONENT));
    return helper.getFirstSelectedCell() instanceof EditorCell_Word && (helper.getTextNode() != null);
  }
  @Override
  public void doUpdate(@NotNull AnActionEvent event, final Map<String, Object> _params) {
    this.setEnabledState(event.getPresentation(), this.isApplicable(event, _params));
  }
  @Override
  protected boolean collectActionData(AnActionEvent event, final Map<String, Object> _params) {
    if (!(super.collectActionData(event, _params))) {
      return false;
    }
    {
      EditorComponent editorComponent = event.getData(MPSEditorDataKeys.EDITOR_COMPONENT);
      if (editorComponent != null && editorComponent.isInvalid()) {
        editorComponent = null;
      }
      if (editorComponent == null) {
        return false;
      }
    }
    return true;
  }
  @Override
  public void doExecute(@NotNull final AnActionEvent event, final Map<String, Object> _params) {
    EditorCell_Word selectedWord = as_kofazl_a0a0a7(event.getData(MPSEditorDataKeys.EDITOR_COMPONENT).getEditorContext().getSelectedCell(), EditorCell_Word.class);
    selectedWord.changeText(selectedWord.getTextAfterCaret());
    if (selectedWord.isCaretPositionAllowed(0)) {
      selectedWord.setCaretPosition(0);
    }
    if (isEmptyString(selectedWord.getText())) {
      selectedWord.deleteWord();
    }
  }
  private static <T> T as_kofazl_a0a0a7(Object o, Class<T> type) {
    return (type.isInstance(o) ? (T) o : null);
  }
  private static boolean isEmptyString(String str) {
    return str == null || str.isEmpty();
  }
}
