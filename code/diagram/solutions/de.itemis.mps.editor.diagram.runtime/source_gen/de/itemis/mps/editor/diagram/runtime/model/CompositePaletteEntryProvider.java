package de.itemis.mps.editor.diagram.runtime.model;

/*Generated by MPS */

import jetbrains.mps.internal.collections.runtime.Sequence;
import java.util.Collections;

public class CompositePaletteEntryProvider implements IPaletteEntryProvider {

  private IPaletteEntryProvider[] myProviders;

  public CompositePaletteEntryProvider(IPaletteEntryProvider... providers) {
    myProviders = providers;
  }

  public Iterable<IPaletteEntry> getEntries() {
    Iterable<IPaletteEntry> result = Sequence.fromIterable(Collections.<IPaletteEntry>emptyList());
    for (IPaletteEntryProvider provider : myProviders) {
      result = Sequence.fromIterable(result).concat(Sequence.fromIterable(provider.getEntries()));
    }
    return result;
  }
}
