package de.slisson.mps.editor.multiline.cells;

/*Generated by MPS */


/**
 * An inclusive range between two integers
 */
public class IntRange {

  private int min;
  private int max;

  public IntRange(int min, int max) {
    if (min < max) {
      this.min = min;
      this.max = max;
    } else {
      this.min = max;
      this.max = min;
    }
  }

  /**
   * Range maximum value, inclusive
   */
  public int getMaximum() {
    return max;
  }

  /**
   * Range minimum value, inclusive
   */
  public int getMinimum() {
    return min;
  }

  public boolean contains(int value) {
    return (value >= min && value <= max);
  }

  public boolean containsRange(IntRange range) {
    if (range == null) {
      return false;
    }
    return contains(range.getMinimum()) && contains(range.getMaximum());
  }

  public boolean overlapsRange(IntRange range) {
    if (range == null) {
      return false;
    }
    return range.contains(min) || range.contains(max) || contains(range.getMinimum());
  }

  public boolean isOverlappedBy(IntRange range) {
    return range.overlapsRange(this);
  }
}
