package com.mbeddr.mpsutil.grammarcells.runtime;

/*Generated by MPS */

import org.jetbrains.mps.openapi.language.SContainmentLink;
import org.jetbrains.mps.openapi.model.SNode;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SConceptOperations;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SNodeOperations;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SLinkOperations;
import org.jetbrains.annotations.Nullable;
import org.jetbrains.mps.openapi.language.SAbstractConcept;
import org.jetbrains.mps.openapi.language.SAbstractLink;

public class ChildSymbol implements ISymbol {

  private SContainmentLink myLink;

  public ChildSymbol(SContainmentLink link) {
    myLink = link;
  }

  @Override
  public boolean matches(IToken element) {
    if (!(element.isChild())) {
      return false;
    }
    SNode childNode = element.getChildNode();
    if ((childNode == null)) {
      return true;
    }
    return SConceptOperations.isSubConceptOf(SNodeOperations.asSConcept(SNodeOperations.getConcept(childNode)), SNodeOperations.asSConcept(myLink.getTargetConcept()));
  }

  @Override
  public void write(IToken element, SNode target) {
    SLinkOperations.setTarget(target, myLink, element.getChildNode());
  }

  @Override
  public IToken read(SNode parent) {
    return new ChildToken(SLinkOperations.getTarget(parent, myLink));
  }

  @Nullable
  @Override
  public PNode parse(InputCursor input, Parser parser, @Nullable SymbolInRule symbolInParent) {
    if (input.isEnd()) {
      return null;
    }
    IToken token = input.getNext();
    if (token != null && matches(token) && input.isEnd()) {
      return new PNode(token, this);
    }
    input.previous();
    return parser.parse(input, myLink.getTargetConcept(), symbolInParent);
  }

  @Override
  public int getNumTokens() {
    return -1;
  }

  @Override
  public boolean hasFixedTokenNumber() {
    return false;
  }

  @Override
  public String toString() {
    return myLink.getName();
  }

  @Nullable
  public SAbstractConcept getOutputConcept() {
    return myLink.getTargetConcept();
  }

  @Nullable
  public SAbstractLink getLink() {
    return myLink;
  }

  public PNode calcCurrentParseTree(SNode parent, Parser parser) {
    PNode pnode = parser.calculateCurrentParseTree(SLinkOperations.getTarget(parent, myLink));
    pnode.setSymbol(this);
    return pnode;
  }

  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || this.getClass() != o.getClass()) {
      return false;
    }

    ChildSymbol that = (ChildSymbol) o;
    if ((myLink != null ? !(myLink.equals(that.myLink)) : that.myLink != null)) {
      return false;
    }

    return true;
  }
  @Override
  public int hashCode() {
    int result = 0;
    result = 31 * result + ((myLink != null ? ((Object) myLink).hashCode() : 0));
    return result;
  }
}
