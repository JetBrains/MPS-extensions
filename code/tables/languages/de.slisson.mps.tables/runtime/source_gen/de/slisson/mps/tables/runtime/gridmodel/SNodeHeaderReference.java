package de.slisson.mps.tables.runtime.gridmodel;

/*Generated by MPS */

import org.jetbrains.mps.openapi.model.SNode;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SNodeOperations;

public class SNodeHeaderReference extends HeaderReference {
  private SNode myNode;

  public SNodeHeaderReference(SNode node) {
    myNode = node;
  }

  @Override
  public SNodeHeaderReference clone() {
    SNodeHeaderReference clone = (SNodeHeaderReference) super.clone();
    return clone;
  }

  @Override
  public boolean equals(Object other) {
    if (!(other instanceof SNodeHeaderReference)) {
      return false;
    }
    return myNode.equals(((SNodeHeaderReference) other).myNode);
  }

  @Override
  public int hashCode() {
    return myNode.hashCode();
  }

  public String getFallbackText() {
    String presentation = SNodeOperations.present(myNode);
    if ((presentation != null && presentation.length() > 0)) {
      return presentation;
    } else {
      return getValue();
    }
  }

  @Override
  public String getValue() {
    return myNode.getNodeId().toString();
  }
}
