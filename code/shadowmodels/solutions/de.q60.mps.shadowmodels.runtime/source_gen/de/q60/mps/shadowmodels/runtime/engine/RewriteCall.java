package de.q60.mps.shadowmodels.runtime.engine;

/*Generated by MPS */

import org.jetbrains.annotations.Nullable;
import org.modelix.model.api.INode;
import org.jetbrains.annotations.NotNull;
import de.q60.mps.polymorphicfunctions.runtime.ParameterList;
import jetbrains.mps.internal.collections.runtime.Sequence;
import java.util.Objects;

public class RewriteCall implements IContainmentTarget, IContainmentTargetWithParameters {
  @Nullable
  public static RewriteCall create(@Nullable INode input, String traceInfo) {
    return (input == null ? null : new RewriteCall(input, traceInfo));
  }

  private INode input;
  private String traceInfo;

  private RewriteCall() {
    // for deserialization
  }

  public RewriteCall(@NotNull INode input, String traceInfo) {
    DependencyTrackingCheck.check(input);
    this.input = input;
    this.traceInfo = traceInfo;
  }

  @Override
  public ParameterList getParameters() {
    return new ParameterList(new Object[]{input});
  }

  public String getTraceInfo() {
    return this.traceInfo;
  }

  @Override
  public Iterable<INode> getInputNodes() {
    return Sequence.<INode>singleton(input);
  }

  public INode getInput() {
    return this.input;
  }

  public TransformationCall toTransformationCall(String transformationId) {
    return toTransformationCall(transformationId, this.traceInfo);
  }

  public TransformationCall toTransformationCall(String transformationId, String traceInfo) {
    return new TransformationCall(transformationId, getParameters(), traceInfo);
  }

  public CopyCall toCopyCall() {
    return new CopyCall(input, traceInfo);
  }

  @Override
  public String toString() {
    return "rewrite(" + input + ")";
  }

  private transient int _cachedHashCode;
  private transient boolean _hashCodeInitialized = false;
  @Override
  public int hashCode() {
    if (!(_hashCodeInitialized)) {
      int c = 0;
      c = 31 * c + Objects.hashCode(input);
      _cachedHashCode = c;
      _hashCodeInitialized = true;
    }
    return _cachedHashCode;
  }
  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || this.getClass() != o.getClass()) {
      return false;
    }
    RewriteCall that = (RewriteCall) o;
    if (!(Objects.equals(input, that.input))) {
      return false;
    }
    return true;
  }
}
