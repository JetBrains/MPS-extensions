package com.mbeddr.mpsutil.grammarcells.runtime;

/*Generated by MPS */

import java.util.Map;
import org.jetbrains.mps.openapi.module.SModuleReference;
import jetbrains.mps.internal.collections.runtime.MapSequence;
import java.util.HashMap;
import jetbrains.mps.smodel.runtime.ModuleDeploymentListener;
import jetbrains.mps.smodel.language.LanguageRegistry;
import org.jetbrains.annotations.NotNull;
import jetbrains.mps.components.ComponentHost;
import org.jetbrains.annotations.Nullable;
import org.jetbrains.mps.openapi.module.SModule;
import jetbrains.mps.smodel.runtime.ModuleDeploymentChange;
import jetbrains.mps.smodel.LanguageAspect;
import jetbrains.mps.baseLanguage.closures.runtime.Wrappers;
import java.util.stream.Stream;
import de.slisson.mps.reflection.runtime.ReflectionUtil;

public class DescriptorCache {

  private static DescriptorCache INSTANCE = new DescriptorCache();

  public static DescriptorCache getInstance() {
    return INSTANCE;
  }

  private final Map<SModuleReference, IGrammarActionsDescriptor> loadedDescriptors = MapSequence.fromMap(new HashMap<>());

  private ModuleDeploymentListener deployListener = null;

  private LanguageRegistry languageRegistry;

  public void invalidate() {
    MapSequence.fromMap(loadedDescriptors).clear();
  }

  public void init(@NotNull ComponentHost mpsPlatform) {
    languageRegistry = mpsPlatform.findComponent(LanguageRegistry.class);
  }

  public void dispose() {
    if (deployListener != null) {
      languageRegistry.removeRegistryListener(deployListener);
      deployListener = null;
    }

    languageRegistry = null;
  }

  @Nullable
  public IGrammarActionsDescriptor getDescriptor(@Nullable SModule module) {
    IGrammarActionsDescriptor descriptor = doGetDescriptor(module);
    if (descriptor != null) {
      if (deployListener == null && languageRegistry != null) {
        deployListener = new ModuleDeploymentListener() {
          @Override
          public void deploymentStateChanged(@NotNull ModuleDeploymentChange change) {
            invalidate();
          }
        };
        languageRegistry.addRegistryListener(deployListener);
      }
    }
    return descriptor;
  }

  @Nullable
  protected IGrammarActionsDescriptor doGetDescriptor(@Nullable SModule module) {
    if (module == null || languageRegistry == null) {
      return null;
    }
    final SModuleReference mref = module.getModuleReference();

    if (MapSequence.fromMap(loadedDescriptors).containsKey(mref)) {
      return MapSequence.fromMap(loadedDescriptors).get(mref);
    }
    final String descriptorClassName = mref.getModuleName() + "." + LanguageAspect.EDITOR.getName() + "." + IGrammarActionsDescriptor.CLASS_NAME;
    final Wrappers._T<IGrammarActionsDescriptor> descriptor = new Wrappers._T<IGrammarActionsDescriptor>(null);

    languageRegistry.withModuleRuntime(Stream.of(mref), (mr) -> {
      try {
        Class<?> descriptorClass = mr.getOwnClass(descriptorClassName);
        descriptor.value = ((IGrammarActionsDescriptor) ReflectionUtil.readField(descriptorClass, null, IGrammarActionsDescriptor.INSTANCE_FIELD_NAME));
      } catch (ClassNotFoundException ex) {
      }
    });

    if (descriptor.value != null) {
      // Module was not loaded or descriptor could not be loaded from module
      MapSequence.fromMap(loadedDescriptors).put(mref, descriptor.value);
    }
    return descriptor.value;
  }
}
