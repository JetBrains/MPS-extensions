package de.itemis.mps.editor.diagram.runtime.jgraph;

/*Generated by MPS */

import org.jetbrains.annotations.ApiStatus;

@ApiStatus.Experimental
public class DiagramControls {
  public static boolean enableAutoLayoutOnInit = true;

  /**
   * Run {@code runnable} with 'autolayout on init', 'fit to size on init', etc. disabled. This is useful in headless
   * batch scenarios where multiple diagrams are being laid out. Since 'autolayout on init' is asynchronous, it would
   * interfere with the batch task.
   * 
   * @param runnable code to run
   */
  public static void runWithoutAutoLayoutOnInit(Runnable runnable) {
    final boolean oldValue = enableAutoLayoutOnInit;
    try {
      enableAutoLayoutOnInit = false;
      runnable.run();
    } finally {
      enableAutoLayoutOnInit = oldValue;
    }
  }
}
