package de.q60.mps.shadowmodels.runtime.engine;

/*Generated by MPS */

import java.util.Objects;

public class ReferenceTargetInParentSubgraph implements IReferenceTarget {
  private IReferenceTarget refTarget;

  private ReferenceTargetInParentSubgraph() {
    // for deserialization
  }

  public ReferenceTargetInParentSubgraph(IReferenceTarget refTarget) {
    this.refTarget = refTarget;
  }

  public IReferenceTarget getRefTarget() {
    return this.refTarget;
  }

  @Override
  public IOutputNodeReference resolve(IScope sourceScope, ITransformationEngine engine) {
    IScope parentStageScope = sourceScope;
    while (Objects.equals(parentStageScope.getStage(), sourceScope.getStage())) {
      parentStageScope = parentStageScope.getParent();
    }
    return refTarget.resolve(parentStageScope, engine);
  }

  private transient int _cachedHashCode;
  private transient boolean _hashCodeInitialized = false;
  @Override
  public int hashCode() {
    if (!(_hashCodeInitialized)) {
      int c = 0;
      c = 31 * c + Objects.hashCode(refTarget);
      _cachedHashCode = c;
      _hashCodeInitialized = true;
    }
    return _cachedHashCode;
  }
  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || this.getClass() != o.getClass()) {
      return false;
    }
    ReferenceTargetInParentSubgraph that = (ReferenceTargetInParentSubgraph) o;
    if (!(Objects.equals(refTarget, that.refTarget))) {
      return false;
    }
    return true;
  }

  @Override
  public String toString() {
    return "../" + refTarget;
  }
}
