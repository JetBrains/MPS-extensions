package de.slisson.mps.editor.multiline.runtime.plugin;

/*Generated by MPS */

import jetbrains.mps.workbench.action.BaseAction;
import javax.swing.Icon;
import jetbrains.mps.workbench.action.ActionAccess;
import com.intellij.openapi.actionSystem.AnActionEvent;
import java.util.Map;
import jetbrains.mps.ide.editor.MPSEditorDataKeys;
import jetbrains.mps.openapi.editor.selection.Selection;
import de.slisson.mps.editor.multiline.cells.MultilineSelection;
import org.jetbrains.annotations.NotNull;
import jetbrains.mps.nodeEditor.EditorComponent;
import jetbrains.mps.openapi.editor.cells.CellActionType;

public class SelectWordRight_Action extends BaseAction {
  private static final Icon ICON = null;

  public SelectWordRight_Action() {
    super("Right with Selection", "", ICON);
    this.setIsAlwaysVisible(false);
    this.setActionAccess(ActionAccess.UNDO_PROJECT);
  }
  @Override
  public boolean isDumbAware() {
    return true;
  }
  @Override
  public boolean isApplicable(AnActionEvent event, final Map<String, Object> _params) {
    //  similar to SelectLeft action from MPS. Most probably this case should be handled there in future
    if (!(event.getData(MPSEditorDataKeys.EDITOR_COMPONENT).isFocusOwner()) || event.getData(MPSEditorDataKeys.EDITOR_COMPONENT).getNodeSubstituteChooser().isVisible()) {
      return false;
    }
    Selection selection = event.getData(MPSEditorDataKeys.EDITOR_COMPONENT).getSelectionManager().getSelection();
    if (!(selection instanceof MultilineSelection)) {
      return false;
    }
    MultilineSelection multilineSelection = (MultilineSelection) selection;
    return multilineSelection.getSelectionEnd() != multilineSelection.getMultilineCell().getTextLength();
  }
  @Override
  public void doUpdate(@NotNull AnActionEvent event, final Map<String, Object> _params) {
    this.setEnabledState(event.getPresentation(), this.isApplicable(event, _params));
  }
  @Override
  protected boolean collectActionData(AnActionEvent event, final Map<String, Object> _params) {
    if (!(super.collectActionData(event, _params))) {
      return false;
    }
    {
      EditorComponent editorComponent = event.getData(MPSEditorDataKeys.EDITOR_COMPONENT);
      if (editorComponent != null && editorComponent.isInvalid()) {
        editorComponent = null;
      }
      if (editorComponent == null) {
        return false;
      }
    }
    return true;
  }
  @Override
  public void doExecute(@NotNull final AnActionEvent event, final Map<String, Object> _params) {
    event.getData(MPSEditorDataKeys.EDITOR_COMPONENT).getSelectionManager().getSelection().executeAction(CellActionType.SELECT_RIGHT);
  }
}
