package de.itemis.mps.editor.math.symbols;

/*Generated by MPS */

import de.itemis.mps.editor.math.runtime.AbstractMathSymbol;
import java.awt.Graphics2D;
import java.awt.geom.Rectangle2D;
import java.awt.BasicStroke;
import java.awt.geom.Path2D;
import java.awt.geom.Line2D;
import java.awt.geom.AffineTransform;
import de.itemis.mps.editor.math.runtime.Dimension2DDouble;
import jetbrains.mps.openapi.editor.TextBuilder;
import jetbrains.mps.editor.runtime.TextBuilderImpl;

public class SumSymbol extends AbstractMathSymbol {

  public SumSymbol() {
  }

  protected void paint(Graphics2D g, Rectangle2D bounds) {
    Graphics2D g2 = (Graphics2D) g.create();
    double lineHalfWidth = 0.075;

    g2.setStroke(new BasicStroke((float) (bounds.getWidth() * lineHalfWidth * 2)));

    Path2D.Double path = new Path2D.Double();
    path.append(new Line2D.Double(1.0, lineHalfWidth, lineHalfWidth, lineHalfWidth), false);
    path.append(new Line2D.Double(lineHalfWidth, lineHalfWidth, 0.75, 0.5), true);
    path.append(new Line2D.Double(0.75, 0.5, lineHalfWidth, 1.0 - lineHalfWidth), true);
    path.append(new Line2D.Double(lineHalfWidth, 1.0 - lineHalfWidth, 1.0, 1.0 - lineHalfWidth), true);

    AffineTransform transform = AffineTransform.getTranslateInstance(bounds.getX(), bounds.getY());
    transform.scale(bounds.getWidth(), bounds.getHeight());
    g2.draw(path.createTransformedShape(transform));
    g2.dispose();
  }
  protected void updateDimension(Dimension2DDouble dimension) {
    dimension.width = Math.pow(dimension.height, 0.7);
  }

  @Override
  public TextBuilder renderText() {
    Rectangle2D bounds = getBounds();
    return new TextBuilderImpl("sum");
  }
}
