package de.itemis.mps.structurecheck.runtime;

/*Generated by MPS */

import org.jetbrains.annotations.Nullable;

public class TypeChecker extends ElementChecker {
  private Class myType;

  public TypeChecker(Class type) {
    myType = type;
  }

  public boolean doesMatch(Object thisElement) {
    if (thisElement == null) {
      return true;
    }
    return myType.isInstance(thisElement);
  }

  @Nullable
  public Message getMessage(Object thisElement) {
    if (doesMatch(thisElement)) {
      return null;
    }
    return new Message("expected type: " + myType.getName() + " / actual: " + thisElement.getClass().getName(), this);
  }
}
