package de.itemis.mps.editor.diagram.runtime.model;

/*Generated by MPS */

import java.util.List;
import org.jetbrains.annotations.Nullable;
import javax.swing.Icon;
import java.awt.Graphics2D;
import java.awt.geom.Rectangle2D;

public interface IConnectionType {
  boolean isValidStart(IConnectionEndpoint endpoint);
  boolean isValidEnd(IConnectionEndpoint endpoint);
  boolean canCreate(IConnectionEndpoint from, IConnectionEndpoint to, List<String> reasons);
  @Nullable
  @Deprecated
  IEdgeAccessor create(IConnectionEndpoint from, IConnectionEndpoint to, IAccessorFactory factory);
  void create(IConnectionEndpoint from, IConnectionEndpoint to);
  String getName();
  Icon getIcon();
  void drawIcon(Graphics2D g, Rectangle2D bounds);
  void extend_AbstractConnectionType_instead_of_implementing_IConnectionType();
  boolean showContextButton();
}
