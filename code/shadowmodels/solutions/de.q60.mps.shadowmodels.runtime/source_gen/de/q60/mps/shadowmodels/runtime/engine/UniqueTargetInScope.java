package de.q60.mps.shadowmodels.runtime.engine;

/*Generated by MPS */

import org.modelix.model.api.INode;
import java.util.Objects;

public class UniqueTargetInScope<E extends IContainmentTarget> implements IUniqueContainmentTarget<E> {
  private IScope scope;
  private IUniqueContainmentTarget<E> target;

  private UniqueTargetInScope() {
    // for deserialization
  }

  public UniqueTargetInScope(IScope scope, IUniqueContainmentTarget<E> target) {
    this.scope = scope;
    this.target = target;
  }

  @Override
  public E getTarget() {
    return target.getTarget();
  }

  @Override
  public Iterable<INode> getInputNodes() {
    return target.getInputNodes();
  }

  @Override
  public IScope getScope() {
    return scope;
  }

  private transient int _cachedHashCode;
  private transient boolean _hashCodeInitialized = false;
  @Override
  public int hashCode() {
    if (!(_hashCodeInitialized)) {
      int c = 0;
      c = 31 * c + Objects.hashCode(target);
      c = 31 * c + Objects.hashCode(scope);
      _cachedHashCode = c;
      _hashCodeInitialized = true;
    }
    return _cachedHashCode;
  }
  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || this.getClass() != o.getClass()) {
      return false;
    }
    UniqueTargetInScope that = (UniqueTargetInScope) o;
    if (!(Objects.equals(target, that.target))) {
      return false;
    }
    if (!(Objects.equals(scope, that.scope))) {
      return false;
    }
    return true;
  }

  @Override
  public String toString() {
    return scope + "/" + target;
  }
}
