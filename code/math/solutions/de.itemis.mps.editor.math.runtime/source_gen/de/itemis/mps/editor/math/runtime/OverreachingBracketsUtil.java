package de.itemis.mps.editor.math.runtime;

/*Generated by MPS */


public class OverreachingBracketsUtil {
  public static void layoutOverreachingBrackets(IMathSymbol leftBracket, MathLayoutableCell body, IMathSymbol rightBracket, double leftInset, double rightInset, int overreachTop, int overreachBottom, int deltaX, int deltaY, boolean alignToCenterY) {
    int bracketTop = 0 + deltaY;
    int bodyDelta = 0;
    if (alignToCenterY && body.getHeight() / 2 > body.getLayoutCenterY()) {
      bodyDelta = ((int) (body.getHeight() - (body.getLayoutCenterY() * 2)));
    }
    double bodyHeight = body.getHeight() + bodyDelta;
    if (alignToCenterY) {
      bodyHeight = Math.max(body.getHeight(), body.getLayoutCenterY() * 2) + bodyDelta;
    }
    double bracketHeight = (bodyHeight + overreachTop + overreachBottom);
    double bracketWidth = bracketHeight / 6;

    leftBracket.setX(0 + deltaX);
    leftBracket.setY(bracketTop);
    leftBracket.setHeight(bracketHeight);
    leftBracket.setWidth(bracketWidth);
    leftBracket.updateDimension();


    body.setX(((int) (deltaX + leftBracket.getWidth() - (bracketWidth * leftInset))));
    body.setY(deltaY + overreachTop + bodyDelta);

    rightBracket.setX(body.getX() + body.getWidth() - (bracketWidth * rightInset));
    rightBracket.setY(bracketTop);
    rightBracket.setHeight(bracketHeight);
    rightBracket.setWidth(bracketWidth);
    rightBracket.updateDimension();
  }
}
