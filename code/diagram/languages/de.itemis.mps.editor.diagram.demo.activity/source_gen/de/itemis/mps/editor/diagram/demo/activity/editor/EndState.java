package de.itemis.mps.editor.diagram.demo.activity.editor;

/*Generated by MPS */

import de.itemis.mps.editor.diagram.runtime.shape.AbstractShape;
import java.awt.Graphics2D;
import java.awt.geom.Rectangle2D;
import de.itemis.mps.editor.diagram.runtime.shape.IShapeStyle;
import java.awt.geom.Ellipse2D;
import com.intellij.ui.JBColor;
import java.awt.BasicStroke;

public class EndState extends AbstractShape {


  public EndState() {
  }



  @Override
  protected void drawShadow(final Graphics2D graphics, final Rectangle2D bounds, IShapeStyle style) {
    double diameter = Math.min(bounds.getWidth(), bounds.getHeight());
    double offsetX = (bounds.getWidth() - diameter) / 2;
    double offsetY = (bounds.getHeight() - diameter) / 2;
    graphics.fill(new Ellipse2D.Double(bounds.getX() + offsetX, bounds.getY() + offsetY, diameter, diameter));
  }

  @Override
  protected void draw(final Graphics2D graphics, final Rectangle2D bounds, IShapeStyle style) {
    graphics.setColor(JBColor.BLACK);
    double diameter = Math.min(bounds.getWidth(), bounds.getHeight());
    double offsetX = (bounds.getWidth() - diameter) / 2;
    double offsetY = (bounds.getHeight() - diameter) / 2;
    graphics.fill(new Ellipse2D.Double(bounds.getX() + offsetX + diameter * 0.1, bounds.getY() + offsetY + diameter * 0.1, diameter * 0.8, diameter * 0.8));

    graphics.setStroke(new BasicStroke((float) (diameter * 0.05)));
    graphics.draw(new Ellipse2D.Double(bounds.getX() + offsetX + diameter * 0.025, bounds.getY() + offsetY + diameter * 0.025, diameter * 0.95, diameter * 0.95));
  }

}
