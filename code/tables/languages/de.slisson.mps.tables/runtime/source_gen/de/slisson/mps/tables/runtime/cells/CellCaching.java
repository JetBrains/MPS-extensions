package de.slisson.mps.tables.runtime.cells;

/*Generated by MPS */

import org.jetbrains.mps.openapi.model.SNode;
import jetbrains.mps.openapi.editor.EditorContext;
import jetbrains.mps.baseLanguage.closures.runtime._FunctionTypes;
import jetbrains.mps.ide.ThreadUtils;
import de.slisson.mps.hacks.editor.EditorCacheHacks;

public class CellCaching {

  private static final String USER_OBJECT_KEY = CellCaching.class.getName() + ".cachingDisabled";

  private static SNode currentNode;
  private static Boolean cachingDisabled;

  public static void maybeDisableCache(SNode node, EditorContext editorContext, final _FunctionTypes._void_P0_E0 body) {
    ThreadUtils.assertEDT();

    if (currentNode != null) {
      body.invoke();
      return;
    }

    try {
      currentNode = node;
      cachingDisabled = as_8c8ovi_a0a1a4a6(node.getUserObject(USER_OBJECT_KEY), Boolean.class);
      if (cachingDisabled == Boolean.TRUE) {
        EditorCacheHacks.noCaching(editorContext, () -> body.invoke());
      } else {
        body.invoke();
      }

      node.putUserObject(USER_OBJECT_KEY, cachingDisabled);
    } finally {
      currentNode = null;
      cachingDisabled = null;
    }
  }

  public static void disableCaching() {
    cachingDisabled = true;
  }

  private static <T> T as_8c8ovi_a0a1a4a6(Object o, Class<T> type) {
    return (type.isInstance(o) ? (T) o : null);
  }
}
