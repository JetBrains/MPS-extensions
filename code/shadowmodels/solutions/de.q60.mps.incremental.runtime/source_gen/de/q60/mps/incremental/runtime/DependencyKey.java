package de.q60.mps.incremental.runtime;

/*Generated by MPS */

import jetbrains.mps.logging.Logger;
import jetbrains.mps.internal.collections.runtime.Sequence;

public abstract class DependencyKey {
  private static final Logger LOG = Logger.getLogger(DependencyKey.class);
  public DependencyKey() {
  }

  public Iterable<Object> getKeys() {
    return Sequence.fromIterable(Sequence.fromArray(getClass().getDeclaredFields())).select((it) -> {
      try {
        it.setAccessible(true);
        return it.get(DependencyKey.this);
      } catch (Exception ex) {
        if (LOG.isErrorLevel()) {
          LOG.error("", ex);
        }
        return null;
      }
    });
  }
}
